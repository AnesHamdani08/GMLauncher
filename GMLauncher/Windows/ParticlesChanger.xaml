<UserControl x:Name="UC" x:Class="ParticlesChanger"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GMLauncher"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Focusable="True">
    <ScrollViewer x:Name="Sc" Width="{Binding Width,ElementName=UC}" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
    <WrapPanel x:Name="Main_Panel" Width="{Binding Width,ElementName=UC}" Orientation="Vertical">
        <Border Width="1500"/>
    </WrapPanel>
    </ScrollViewer>
</UserControl>
